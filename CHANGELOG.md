# 更新日志

本项目遵循语义化版本的基本原则，版本号与 Docker 镜像标签保持一致。

## Unreleased

- 新增 Flutter GitHub Actions 自动发版流程：推送 `vMAJOR.MINOR.PATCH` 标签后自动构建 H5、Android、macOS、Windows 和未签名 iOS 产物，并发布到 GitHub Release。
- Android Release 构建优先通过 GitHub Secrets 注入固定 keystore；Secrets 尚未配置时使用临时 CI key，避免其他平台构建完成后因 Android 配置缺失而无法发布 Release。
- 固定 Windows runner 版本并补齐 Flutter 原生插件链接，降低 Windows hosted runner 的 CMake 构建失败概率。

## 1.15.3

- 修复 iPhone 17 Pro Max 主屏幕模式下“添加内容”弹层覆盖状态栏、标题和关闭按钮过高的问题。
- 移除独立运行模式对弹层高度的错误覆盖，让弹层统一从顶部安全区下方开始，并保留额外触控间距。

## 1.15.2

- 修复 iPhone Safari 与主屏幕模式顶部安全区被挤进固定导航高度、导致状态栏和页面内容重叠的问题。
- 顶部导航、侧栏、共享页和文件预览统一使用动态安全距离；iOS 主屏幕模式在系统未返回安全区时仍保留 54px 兜底。
- PWA 静态缓存升级并强制检查新版服务工作线程，降低手机继续显示旧样式的概率。

## 1.15.1

- 手机端预览面板完整适配 iPhone 安全区，关闭按钮与顶部、底部系统区域保持可触达距离。
- 概览、文件列表和个人 WebDAV 支持原生手感的下拉刷新；Safari 可从左侧边缘右滑返回上一级或关闭预览。
- 图片预览会根据屏幕和旋转方向自动缩放、居中，并支持双指缩放；PDF 与 Office 转换预览默认适配页面宽度。
- 个人 WebDAV 的刷新和返回逻辑与当前目录联动，不会意外跳回根目录。
- Docker 运行时升级到 Node.js 26，规避 Node.js 24 下加密 SQLite 原生模块偶发的清理断言和重复重启。

## 1.15.0

- 新增独立 Flutter 客户端，复用现有 Express 后端并支持 H5、Android 与 iOS 工程构建。
- Flutter 客户端支持登录、文件夹、上传、粘贴、收藏、分享、回收站及私有文件预览与下载。
- 完善 Flutter 的“我的分享”：支持复制/打开链接、查看有效期、延长有效期和关闭分享；收藏与回收站同时支持文件夹。
- 回收站支持文件和文件夹恢复、永久删除及级联操作，并在永久删除前增加二次确认。
- 优化 Android/H5 页面切换：主导航取消默认转场，文件请求增加竞态保护和局部加载状态，避免旧请求覆盖新页面。
- 关闭 Flutter Debug 模式下的黑黄调试标识、性能面板、网格和棋盘格诊断覆盖层。
- 修复 Android 窄屏下“我的分享”“收藏”“回收站”空状态的 RenderFlex 溢出，空状态改为可滚动布局。
- Flutter 客户端默认使用正式 API `https://ccopy.cloud123.uk:333`，可通过 `API_MODE=local` 和 `LOCAL_API_BASE_URL` 切换本地开发接口。
- Flutter 客户端首页新增“设置”，支持保存外网、内网和自定义服务端地址，测试连接后运行时切换 API，并支持 Android 真机填写局域网 IP。

- 首页升级为“一处复制，随处粘贴”的全新品牌入口和跨设备主视觉。
- 突出浏览器即开即用：文件不主动下载就不占手机文件空间，无需额外安装 App。
- 强化一条限时链接分享给任何人的产品定位，并展示文件管理与多格式预览优势。
- 手机登录页加入精简品牌承诺，在不影响登录效率的前提下说明轻量使用方式。
- 统一更新 GitHub README、网页分享元数据、PWA 描述和 Docker Hub 宣传页面。

## 1.14.0

- 全站字号会根据视口尺寸在安全范围内自动放大或缩小，兼顾大屏、横屏与手机阅读。
- 预览面板支持沉浸式展开、图片缩放旋转、文本复制和 JSON 自动格式化。
- 扩展 Office/WPS 模板、宏文档、幻灯片放映、Visio，以及常见代码、配置和数据文件识别。
- 新增森屿、落霞和鸢尾皮肤，总计提供 6 套协调的界面主题。
- 新增简体中文、繁体中文、英语、日语、韩语、西班牙语和法语切换，日期随语言显示。
- 个人 WebDAV 预览同步增强，并补充扩展格式自动化回归测试。

## 1.13.0

- 概览文本点击后同时复制内容并打开预览。
- 新增独立头像中心和 20 个全彩卡通动物头像。
- 修复管理中心“版本与配置”页面的路由和搜索框污染问题。
- 强化手机端布局，加入底部导航、底部操作面板、触控优化和安全区适配。
- 新增 PWA Manifest、应用图标和静态资源 Service Worker。
- 优化 Docker 构建缓存和版本标签一致性。

## 1.12.0

- 加固文件分享、短期访问凭据与用户所有权校验。
- 公共分享响应移除内部存储标识，并增加统一限速。
- 强化 WebDAV 地址校验，阻止常见云元数据目标。
- 扩展 Office、WPS、PDF 与媒体文件预览。

## 1.11.0

- 合并上传文件与粘贴文本入口。
- 优化个人 WebDAV 与“我的文件”之间的双向文件操作。
- 改进共享有效期展示、菜单行为和预览一致性。
- 增加用户自定义头像和个人资料管理。

## 1.10.0

- 修复删除后的界面刷新和文件夹返回上一级操作。
- 强化 Word、Excel、PowerPoint、PDF 与 WPS 文档转换预览。
- 重新设计管理中心的版本与配置页面。

## 1.9.0

- 加入概览滚动续载、每页数量和视图切换。
- 将个人设置与 WebDAV 设置分区。
- 完成个人 WebDAV 远端文件管理器。
- 修复管理中心子路由恢复问题。

## 1.7.0–1.8.0

- 概览支持直接查看并复制文本内容。
- 每位用户可配置多个个人 WebDAV 连接。
- 个人 WebDAV 支持上传、新建目录、重命名、复制、移动、删除与预览。
- 文件选择工具栏增加发送到 WebDAV。

## 1.5.0–1.6.0

- “我的文件”升级为带文件夹和多视图的桌面式文件管理器。
- 加入复制、剪切、粘贴、重命名、复制到和移动到。
- 增加可配置的到期提醒和收藏永久保留。
- 增加加密配置导出、验证和恢复。
- 账号设置支持修改邮箱、自助注销和管理员删除账号。
